class DurationManager {
  DurationManager._();

  static const Duration twoSeconds = Duration(seconds: 2);
  static const Duration threeSeconds = Duration(seconds: 3);
  static const Duration hundredMillieSecond = Duration(milliseconds: 100);
  static const Duration threeHundredsMillieSecond = Duration(milliseconds: 300);
  static const Duration onboardingAnimatePageDuration = Duration(milliseconds: 300);
  static const Duration onboardingAnimateDotIndicatorDuration = Duration(milliseconds: 400);
}
